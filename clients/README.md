# cliptown client SDKs

These runtime-specific SDK baselines depend on the `cliptown-interfaces`
and `cliptown-lib` Zed packages. Existing product bindings are preserved;
missing targets receive a transport-neutral client configuration baseline.
